apply plugin: 'com.android.library'

android {
  compileSdkVersion androidTargetSdk
  buildToolsVersion androidBuildTool

  defaultConfig {
    minSdkVersion androidMinSdk
  }

}

dependencies {
  compile 'com.android.support:design:' + androidSupportLib
}
